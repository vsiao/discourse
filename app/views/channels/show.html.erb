<script>
$(function() {
  var faye = new Faye.Client('http://128.237.116.88:9292/faye');
  faye.subscribe("/messages/<%= @channel.name %>", function(data) {
    eval(data);
  });
});
</script>

<p id="notice"><%= notice %></p>

<% if user_signed_in? %>
<p>
Your Name: <%= current_user.name %>
</p>
<% end %>

<p>
  <b>Name:</b>
  <%= @channel.name %>
</p>

<p>
  <b>School:</b>
  <%= @channel.school_id %>
</p>

<% title "Chat" %>

<ul id="chat">

</ul>

<%= form_for Message.new, :remote => true do |f| %>
  <%= f.hidden_field :channel_name, :value => @channel.name %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>


<%= link_to 'Edit', edit_channel_path(@channel) %> |
<%= link_to 'Back', channels_path %>
