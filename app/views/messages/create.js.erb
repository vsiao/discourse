<% broadcast "/channels/" + @chan_id do %>
  $("<%= escape_javascript render(@message) %>").hide().appendTo('#chat-<%= @chan_id %>').fadeIn(200);
  $('html, body').animate({scrollTop: $(document).height()}, 300);
<% end %>
$("#new_message_<%= @chan_id %>")[0].reset();
